%h1 Greetings! #{current_user.email} Let's create a new organization!
.row
  .col-md-6.col-md-offset-3
    = form_with model: @organization, local: true do |f|

      = f.hidden_field :user_id
      = f.label :title
      = f.text_field :title
      - @organization.errors.full_messages_for(:title).each do |message|
        %div= message
      = f.submit "Create new organization", class: "btn btn-primary"